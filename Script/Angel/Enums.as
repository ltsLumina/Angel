enum EBodyPart
{
    Head,
    Body,
    Legs
};

enum EEquipSpeed
{
    Normal,
    Fast,
    Instant
};

enum EFireMode
{
    Semi = 0,
    Burst = 1,
    Auto = 2,
};

enum EAngelMovementState
{
    Still,
    Airborne,
    Crouch,
    Run,
    Walk,
    CrouchWalk
};